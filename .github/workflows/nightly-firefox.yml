name: Nightly Tests - Firefox

on:
  schedule:
    # Run nightly at 2:00 AM EST (7:00 AM UTC)
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  test-firefox:
    # 🌙 Nightly Firefox Tests
    # Runs automated tests against Firefox browser
    # Schedule: Daily at 2:00 AM EST (7:00 AM UTC)
    uses: ./.github/workflows/test-by-browser.yml
    with:
      browser: firefox
    secrets: inherit
